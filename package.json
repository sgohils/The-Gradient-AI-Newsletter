{
  "name": "the-gradient-ai-newsletter",
  "version": "0.1.0",
  "description": "An AI Newsletter with daily updates about the latest news in AI",
  "main": "dist/scripts/run.js",
  "scripts": {
    "build": "tsc",
    "dev": "npm run build && node dist/scripts/run.js",
    "publish": "npm run build && node dist/scripts/run.js",
    "backfill-image": "npm run build && node dist/scripts/backfill-image.js",
    "test": "vitest run",
    "lint": "eslint src --ext .ts"
  },
  "keywords": [
    "ai",
    "newsletter",
    "rss",
    "automation"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.0",
    "cheerio": "^1.0.0-rc.12",
    "date-fns": "^3.0.0",
    "dotenv": "^16.3.0",
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {
    "@types/cheerio": "^1.0.0",
    "@types/node": "^20.10.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.55.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
